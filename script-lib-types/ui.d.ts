/**
 * # ui library
 * 
 * Including this library with `useLibrary("ui")` is a shortcut to including
 * the main user interface (UI) libraries: [[uibindings]], [[uicontrols]],
 * and [[uilayout]].
 */
declare module ui {
}